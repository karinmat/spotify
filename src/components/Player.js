export default function Player() {
    return (
        <div>
            <button> prev </button>
            <button> play </button>
            <button> next </button>
        </div>
    );
}
